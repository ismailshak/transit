# The `incidents` command

Displays any disruptions or delays across all stations, as reported by your local transit authority.

```bash
transit incidents
```

or you can use the alias

```bash
transit inc
```
